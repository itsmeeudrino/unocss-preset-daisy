import type { Preset } from 'unocss'

// Port of packages/daisyui/src/utilities/*.css: glass, join, radius, typography
// TODO(P4)
export function utilityRules(_opts: { prefix: string }): Preset['rules'] {
  return []
}
