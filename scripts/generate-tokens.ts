// P1: upstream themes -> src/theme/tokens.ts bridge.
// Reads packages/daisyui/src/themes/*.css variable names, emits
// `{ 'primary': 'var(--color-primary)', ... }` + depth/noise/radius/size vars.
// Usage: bun run tokens
console.log('TODO(P1): implement theme token generation.')
