{
  "name": "unocss-preset-daisy",
  "version": "5.7.0-uno.0",
  "description": "daisyUI v5 ported to UnoCSS — zero-Tailwind preset (hard fork of daisyui@5.7.43)",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" },
    "./themes": "./dist/themes.css"
  },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "bun build.ts",
    "lint": "tsc --noEmit",
    "format": "bunx @biomejs/biome@2.5.9 format --write src tests scripts build.ts uno.config.ts playground/serve.ts",
    "test": "bun test",
    "check": "bun run build && bun run lint && bun test",
    "inventory": "bun scripts/inventory-apply.ts",
    "tokens": "bun scripts/generate-tokens.ts",
    "playground": "bun playground/serve.ts"
  },
  "peerDependencies": {
    "unocss": ">=0.60.0"
  },
  "devDependencies": {
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
